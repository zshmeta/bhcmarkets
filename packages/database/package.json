{
    "name": "@repo/database",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "main": "src/index.ts",
    "types": "src/index.ts",
    "exports": {
        ".": "./src/index.ts",
        "./schema": "./src/schema/index.ts"
    },
    "scripts": {
        "db:generate": "bun run build && drizzle-kit generate",
        "db:migrate": "bun run build && drizzle-kit migrate",
        "db:push": "bun run build && drizzle-kit push",
        "build": "tsc",
        "lint": "eslint .",
        "typecheck": "tsc --noEmit"
    },
    "dependencies": {
        "drizzle-orm": "catalog:",
        "ioredis": "catalog:",
        "pg": "catalog:",
        "postgres": "catalog:"
    },
    "devDependencies": {
        "@repo/typescript-config": "*",
        "@types/node": "^25.0.6",
        "@types/pg": "catalog:",
        "drizzle-kit": "catalog:",
        "eslint": "catalog:",
        "typescript": "catalog:"
    }
}
